{
  "name": "minimal-pkgs",
  "blueprint": {
    "name": "minimal-pkgs",
    "description": "Simple blueprint that enables the (experimental) minimal feature, which reduces the OS package list to the essentials. Includes extra user-selected packages.",
    "version": "1.0",
    "minimal": true,
    "packages": [
      {
        "name": "tmux"
      },
      {
        "name": "vim-enhanced"
      }
    ]
  }
}
