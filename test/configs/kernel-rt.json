{
  "name": "kernel-rt",
  "blueprint": {
    "name": "kernel-rt",
    "description": "Simple blueprint with an alternative kernel (kernel-rt). Requires the RT repository.",
    "version": "1.0",
    "customizations": {
      "kernel": {
        "name": "kernel-rt"
      }
    }
  }
}
