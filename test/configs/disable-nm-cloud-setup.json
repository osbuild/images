{
  "name": "disable-nm-cloud-setup",
  "blueprint": {
    "name": "disable-nm-cloud-setup",
    "description": "The nm-cloud-setup service fails to start on RHEL 9.0 aarch64 vhd (RHEL-112357). We use this config to disable the service so it doesn't fail our boot tests.",
    "version": "1.0",
    "customizations": {
      "services": {
        "disabled": [
          "nm-cloud-setup.service"
        ]
      }
    }
  }
}
